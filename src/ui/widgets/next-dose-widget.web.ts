import type { NextDoseWidgetProps } from '@/models/next-dose-widget';

const NextDoseWidget = {
  updateTimeline: (_entries: { date: Date; props: NextDoseWidgetProps }[]) => undefined,
};

export default NextDoseWidget;
